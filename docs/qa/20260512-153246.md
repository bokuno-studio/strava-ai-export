# QA レポート 2026-05-12 15:32:46

## メタ情報
- コミット: b95357df7f45896f89f360fcec373d1f1d663067
- ブランチ: main
- 実行日時: 2026-05-12 15:32:46
- プロジェクト種別: 未判定（プロジェクト雛形のみ・実装未着手）

## 要件ソース
- 使用したファイル:
  - `CLAUDE.md`
  - `docs/onboarding.md`（API ピボット反映済み、2026-05-11 更新）
  - `docs/scenarios.md`（既存。API ベースに改訂済み）
  - `.gitignore`
  - `git log --oneline -20`（コミット 2 件: `3d32c24` Initial commit / `b95357d` docs: pivot to Strava API + add PHR long-term vision）
  - `gh issue list`（git remote 未設定のため不可・スキップ）
- シナリオ: 既存（`docs/scenarios.md` を変更せず使用）

## シナリオと結果

実装ファイル（`package.json` / `src/` / `next.config.*` 等）が一切存在しないため、全シナリオを skip。
シナリオ集自体は 2026-05-11 の API ピボットに合わせて改訂済みで、次回 QA 以降そのまま流用可能。

### トップページ表示（未認可）
- 結果: ⏭ skip
- 詳細: アプリ未実装。

### OAuth 認可（ハッピーパス）
- 結果: ⏭ skip
- 詳細: アプリ未実装。OAuth エンドポイント（`/api/auth/start` / `/api/auth/callback`）なし。

### OAuth 拒否
- 結果: ⏭ skip
- 詳細: アプリ未実装。

### ホーム画面追加（任意・できる人向け）
- 結果: ⏭ skip
- 詳細: `manifest.json` 未設置・本番デプロイ未実施。

### 「最新 1 件」取得（運動直後・主要ハッピーパス）
- 結果: ⏭ skip
- 詳細: 同期ワーカー・UI ともに未実装。

### 「直近 7 日」取得
- 結果: ⏭ skip
- 詳細: 同上。

### 「全期間」初回フル取得（ハッピーパス）
- 結果: ⏭ skip
- 詳細: 同上。

### 「全期間」取得中にタブを閉じる → 再開
- 結果: ⏭ skip
- 詳細: `sync_cursor` 等のサーバ DB 未構築。

### アプリ全体レート制限ヒット（混雑時）
- 結果: ⏭ skip
- 詳細: サーバ実装が無いため `X-RateLimit-Usage` 監視ロジック未存在。

### ユーザー単独レート制限ヒット
- 結果: ⏭ skip
- 詳細: 同上。

### 差分 CSV ダウンロード
- 結果: ⏭ skip
- 詳細: CSV/ZIP 生成パイプライン未実装。PapaParse / JSZip 未導入。

### AI ごとのアップロード方法ガイド
- 結果: ⏭ skip
- 詳細: UI 未実装。

### 異常系 - token 失効
- 結果: ⏭ skip
- 詳細: 同期実装なし。

### 異常系 - Strava 障害
- 結果: ⏭ skip
- 詳細: 同上。

### 異常系 - IndexedDB 容量超過
- 結果: ⏭ skip
- 詳細: IndexedDB スキーマ未定義。

### モバイル UI（375px）
- 結果: ⏭ skip
- 詳細: UI 未実装。

### セキュリティヘッダ
- 結果: ⏭ skip
- 詳細: 本番デプロイ未実施・`next.config.ts` 未作成。

### token の保管
- 結果: ⏭ skip
- 詳細: サーバ DB 未構築（Supabase / Vercel KV の選定もこれから）。

### favicon / OG / sitemap / llms.txt
- 結果: ⏭ skip
- 詳細: 本番デプロイ未実施・該当アセット未配置。

## 環境
- 起動コマンド: なし（`package.json` 不在で `npm run dev` 不可）
- 起動結果: 失敗（プロジェクト未初期化）

## 詰まった箇所
- リポジトリにはまだ `CLAUDE.md` / `.gitignore` / `docs/` しか存在せず、Next.js プロジェクトの雛形（`package.json`、ソースコード、テスト設定）が一切ない。前回 QA（2026-05-11 16:20）と同じ状態。
- 直近コミット `b95357d` は「docs: pivot to Strava API + add PHR long-term vision」で、入力方式を ZIP → Strava API に切り替えた**ドキュメント変更のみ**。実装着手は依然として未着。
- 上記により Step 4（環境起動）・Step 5（テスト実行）はいずれも対象が存在せず実施不可。
- GitHub remote が未設定のため `gh issue list` での要件補完もできなかった。

## 総合判定
- 0 / 19 シナリオ pass（19 件すべて skip）
- 判定: ⚠️ 要確認

### 要修正・要確認の内容
- **プロジェクト雛形未作成**: `docs/onboarding.md §12` の手順 2「Next.js + Tailwind の雛形作成」が未着手。`package.json` 生成・依存導入（Next.js / Tailwind / JSZip / PapaParse / FileSaver.js）が次の最小マイルストーン。
- **Strava OAuth アプリ未登録**: `docs/onboarding.md §12` の手順 1「Strava の OAuth アプリ登録（client_id / client_secret 取得）」が未対応。これが無いと OAuth フローを実装しても動作確認できない。
- **サーバ DB 未選定**: `docs/onboarding.md §6 / §12` の Supabase or Vercel KV の最終確定がまだ。token 暗号化保管・`sync_cursor` 保存の前提。
- **GitHub remote 未設定**: 運用ワークフロー（Issue 起票 → triage → ready → ...）が依然として機能しない。リモートリポジトリ作成と `git remote add` が必要。
- 上記が解消され次第、`docs/scenarios.md` をベースに本格 QA を再実行する。
